// Package config resolves wzrd-vault configuration from XDG paths,
// environment variables, and config files. It centralizes all path
// and configuration resolution so commands never hardcode paths.
package config
